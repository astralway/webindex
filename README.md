# Common Crawl

### Requirements

    * Hadoop (HDFS & YARN)

### Load Data

Data can be downloaded from AWS and loaded in HDFS using the following command:

    ./bin/load.sh <numFiles>

### Run WordCount M/R

    ./bin/wordcount.sh
